/* React native paper. */
import { DefaultTheme } from 'react-native-paper'

const theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    background: '#FFFFFF',
    primary: '#4C9DAF',
    placeholder: '#7D8286'
   },
}

export { 
    theme 
}