/* React native paper. */
import { DefaultTheme } from 'react-native-paper'

const theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    background: '#FFFFFF',
    primary: '#123C69',
    accent: '#BAB2B5',
    transparency: '#CAE2E8',
    placeholder: '#7D8286'
   },
}

export { 
  theme 
}